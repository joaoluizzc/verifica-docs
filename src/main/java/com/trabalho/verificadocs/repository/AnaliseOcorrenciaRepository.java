package com.trabalho.verificadocs.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.trabalho.verificadocs.model.AnaliseOcorrencia;

public interface AnaliseOcorrenciaRepository extends JpaRepository<AnaliseOcorrencia, Long> {
}
